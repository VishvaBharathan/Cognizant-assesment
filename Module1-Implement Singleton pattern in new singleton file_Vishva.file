class Singleton:
    _obj = None

    def __new__(cls):
        if cls._obj == None:
            cls._obj = super(Singleton, cls).__new__(cls)
        return cls._obj

first = Singleton()
second = Singleton()

print(id(first) == id(second))
